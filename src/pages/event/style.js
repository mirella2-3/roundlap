import styled from 'styled-components';

export const EventStyle = styled.div`
    .inner {
        padding-top: 160px;
        text-align: center;

        h1 {
            display: inline-block;
            margin: 0 auto;
            font-weight: 700;
            color: #1a1a1a;
            font-size: 48px;
        }
    }
`;
